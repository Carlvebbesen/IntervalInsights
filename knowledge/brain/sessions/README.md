# sessions/

Concrete, repeatable **workout templates** (`type: session`). A session composes warm-up,
a main set (often reusing an `[[interval]]`), and cool-down, with intensity targets.

Add one with `/brain-ingest`. Use `../_templates/session.md`. Empty for now.
