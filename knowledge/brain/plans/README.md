# plans/

**Active training programs** (`type: plan`) — the PARA "Projects" layer. A plan has a goal,
a timeframe, phases (`[[periodization]]`), and a weekly template. When finished, move it to
`../archive/` and set `status: archived`.

Add one with `/brain-ingest`. Use `../_templates/plan.md`. Empty for now.
